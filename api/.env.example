# ========================================
# ChâTop API - Environment Variables Template
# ========================================
#
# This file serves as a template for environment variables.
# Copy this file to .env and fill in your actual values.
#
# IMPORTANT: Never commit .env file to version control!
# ========================================

# ----------------------------------------
# Database Configuration
# ----------------------------------------
# MySQL database connection URL
DB_URL=jdbc:mysql://localhost:3306/chatop

# MySQL database username
DB_USERNAME=root

# MySQL database password (leave empty if no password)
DB_PASSWORD=

# ----------------------------------------
# JWT Configuration
# ----------------------------------------
# JWT Secret Key - MUST be changed in production!
# Generate a secure random string (recommended: 256+ bits)
# You can generate one using: openssl rand -base64 64
JWT_SECRET=changeme-generate-a-secure-key-for-production

# JWT Token expiration time in milliseconds
# Default: 86400000 (24 hours)
JWT_EXPIRATION=86400000

# ----------------------------------------
# Notes
# ----------------------------------------
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual credentials
# 3. Never commit .env to git (already in .gitignore)
# 4. For production, use strong passwords and secrets
# 5. See ENVIRONMENT_SETUP.md for detailed configuration guide
